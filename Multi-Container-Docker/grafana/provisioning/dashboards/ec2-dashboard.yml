apiVersion: 1
providers:
  - name: 'Infrastructure Dashboards'
    orgId: 1
    folder: 'Infrastructure' # This creates the folder name in the UI
    type: file
    options:
      path: /etc/grafana/dashboards